subdir('builtins')
subdir('parser')
subdir('utils')
subdir('terminal')

lib_src = files(
  'error.cpp',
  'repl.cpp',
)

lib_src += builtins_src
lib_src += parser_src
lib_src += utils_src
lib_src += terminal_src